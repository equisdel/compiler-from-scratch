## Este programa de prueba tiene errores de declaración de variables y funciones.

programap3
begin

single sing1 , sing2;

outf([impresion]);

sing1,sing2 := 1.2s+2, 1.2s-2;

#prueba de if y if else : cond, parentesis, cuerpo, errores random (Que deberia comerse todo el if y seguir..)

uinteger fun testeveryverylarge(uinteger intval)
begin
    if intval > 10 then
        goto SALTITO@;
    else
        RET (intval * intval); 
    end_if;
end
uinteger aux;
aux := testeveryverylarge(5);
SALTITO@
outf(aux);

repeat begin
aux := aux + 1;
end
until (aux = 10);

end
