## Este programa de prueba tiene errores de declaración de variables y funciones.

programap3
begin

single sing1 , sing2;

outf([impresion]);

sing1,sing2 := 1.2s+2, 1.2s-2;

uinteger fun testeveryverylarge(uinteger intval)
begin
    if (intval > 10) then
        goto SALTITO@;
    end_if;
    RET (intval * intval);
end
uinteger aux;
aux := testeveryverylarge(5);
SALTITO@
outf(aux);

#Deteccion de varios errores del repeat until: falta de cond, de parentesis, de PR, de sentencia ejecutable...
# SI FALTA CONDICION DA ERROR DE EXPR ?? SOLO DEVUELVE Q FALTA CONDICION, SI HAY PARENTESIS VACIOS

repeat begin

end
until aux = 10)

end
